export enum ProductStatus {
  instock = 'instock',
  outofstock = 'outofstock',
}

export enum OrderItemStatus {
  new = 'new',
  processing = 'processing',
  ready = 'ready',
  pickedup = 'pickedup',
  cancelled = 'cancelled',
}
